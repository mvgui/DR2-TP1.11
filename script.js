//Entrada
let tempo = Number(prompt("Digite o tempo em segundos:"));
let distancia = Number(prompt("Digite a distância em metros:"));

//Processamento
let velocidadeMedia = distancia / tempo;

//Saída
alert("A velocidade média é de " + velocidadeMedia + "m/s.");
